var Enum = { }

// Boilerplate: use enums rather than hardcoded strings / ints whenever possible

Enum.Example = {
  ThisThing     : 0,
  ThatThing     : 1,
  SomethingElse : 2
}

module.exports = Enum
